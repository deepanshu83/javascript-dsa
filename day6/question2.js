// que 2 :- how to check if an object is an array are not??
// in this que we learn and use 
// Array.isArray()
// typeof

function checkArr(elem){
     console.log(Array.isArray(elem));
}

checkArr([]);
checkArr({});